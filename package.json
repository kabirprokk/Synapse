{
  "name": "synapse",
  "private": true,
  "version": "1.0.0",
  "description": "Synapse Neuro-Evolution Arena — Q-Learning vs Minimax+Genetic Tic-Tac-Toe lab. Seeded, measurable, human-playable.",
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "dev": "vite --port=3000 --host=0.0.0.0",
    "build": "vite build",
    "preview": "vite preview",
    "clean": "rm -rf dist",
    "lint": "tsc --noEmit",
    "test": "vitest run",
    "deploy": "npm run build && firebase deploy --only hosting,firestore",
    "deploy:hosting": "npm run build && firebase deploy --only hosting",
    "deploy:main": "npm run build && firebase deploy --only hosting:synarena",
    "deploy:synarena": "npm run build && firebase deploy --only hosting:synarena",
    "deploy:default-site": "npm run build && firebase deploy --only hosting:synapse-ai-inovation"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.1.14",
    "@vitejs/plugin-react": "^5.0.4",
    "clsx": "^2.1.1",
    "firebase": "^12.19.0",
    "framer-motion": "^13.2.0",
    "lucide-react": "^0.546.0",
    "motion": "^12.23.24",
    "react": "^19.0.1",
    "react-dom": "^19.0.1",
    "tailwind-merge": "^3.7.0",
    "vite": "^6.2.3"
  },
  "devDependencies": {
    "@types/node": "^22.14.0",
    "autoprefixer": "^10.4.21",
    "esbuild": "^0.25.0",
    "tailwindcss": "^4.1.14",
    "typescript": "~5.8.2",
    "vite": "^6.2.3",
    "vitest": "^2.1.0"
  }
}
